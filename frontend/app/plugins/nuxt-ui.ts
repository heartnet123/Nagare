/**
 * Nuxt UI plugin — ensures UI module is properly configured.
 * The theme is defined in app.config.ts; this file exists
 * for any runtime UI plugin registration.
 */
export default defineNuxtPlugin(() => {
  // UI components are auto-imported by @nuxt/ui module
})
